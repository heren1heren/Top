<head>
    <link rel="stylesheet" href="/styles.css">
</head>
<html>

<body>

    <%- include('navbar', {links: links}) %>
        <ul>
            <% users.forEach((user)=> { %>
                <%- include('users/user', {user}) %>

                    <% }); %>

        </ul>
</body>

</html>